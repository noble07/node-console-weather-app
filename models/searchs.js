class Searchs {
  constructor() {
    //TODO: Read DB if exist
  }

  async city(place = '') {
    //TODO Petición HTTP
    console.log(place)

    return [] // Return places that matched with the parameter
  }
}

module.exports = Searchs